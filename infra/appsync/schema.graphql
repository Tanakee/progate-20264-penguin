type Comment {
  id: ID!
  content: String!
  color: String
  createdAt: AWSDateTime!
}

type Mutation {
  sendComment(content: String!, color: String): Comment!
}

type Subscription {
  onComment: Comment @aws_subscribe(mutations: ["sendComment"])
}

type Query {
  _empty: String
}
